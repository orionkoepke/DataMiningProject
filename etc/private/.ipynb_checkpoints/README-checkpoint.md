# private

This folder holds **private keys and credentials** (e.g. `alpaca_key.json` for Alpaca API access).

All files in this directory are git-ignored. **Never commit real API keys or secrets.** Copy your keys here locally after cloning; they will not be tracked by git.
