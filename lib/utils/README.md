# utils

This subdirectory holds **generic utility code** (e.g. conversions, helpers) used across the project.

Use this module from experiments or other lib code (e.g. `from lib.utils import ...`) when you need shared utilities that are not specific to stock data.
