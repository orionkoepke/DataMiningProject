# integration

**Integration tests** for code in `lib`.

Integration tests can be **slower** and **may rely on outside entities** (e.g. pulling real data from a stock broker, calling live APIs). Use them to verify that components work together with real services; run them when you need that coverage or before releases.
